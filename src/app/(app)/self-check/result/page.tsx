import { SelfCheckResult } from "@/components/self-check/self-check-result";

export const metadata = {
  title: "진단 결과 — Globable",
};

export default function SelfCheckResultPage() {
  return <SelfCheckResult />;
}
